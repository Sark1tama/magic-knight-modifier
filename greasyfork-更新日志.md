# 📜 魔法骑士修改器 更新日志

> 脚本说明与功能介绍见 [greasyfork-发布说明.md](greasyfork-发布说明.md)
> **当前版本**:v3.9.11

- **v3.9.11**：高级页技能倍率的英文键名（fireBall/laser 等）改为显示中文技能名——实测倍率表、运行时 token 表、服务器技能列表三者按键序一致（获得顺序），按序对齐翻译，不硬编码映射表；对不齐时回退英文键
- **v3.9.10**：修复信息页"当前构筑"不更新——此前读的是 launchData 里的开局快照（整场不变），改为读战斗组件的 `serverActiveSkills`/`serverPassiveSkills` 实时数据（还自带中文技能名，不再依赖索引翻译）——此前读的是 launchData 里的开局快照（整场不变），改为读战斗组件的 `serverActiveSkills`/`serverPassiveSkills` 实时数据（还自带中文技能名，不再依赖索引翻译）
- **v3.9.9**：战斗模式新增 📋 信息 tab 并设为默认首页——原基础页顶部的只读信息移入其中，并新增战斗计时（游戏内时钟 02:51 格式）、击杀数、本局金币、局内等级、当前技能构筑（主动/被动技能中文名+等级）五项实时信息；基础页从此只留修改项
- **v3.9.8**：战斗信息"技能类型"从裸数字 ID 改为显示固有技能名（如"火球 (4)")——取自游戏自身解析好的技能索引，索引未就绪时回退显示数字
- **v3.9.7**：缩小脚本运行范围——`@match` 从全站改为仅直播间页面（数字房间号/beta/topic），其他斗鱼页面不再弹出面板；保留游戏 iframe 路径匹配以确保"防后台暂停"补丁照常注入
- **v3.9.6**：高级页新增"🧹 清除掉落/敌人记忆"按钮（与"恢复默认"并列）——掉落率/火焰伤害/血瓶回血比例/敌人上限不在游戏的原始值快照里，改过就会被记住并每场重放；此按钮清除这些项的记忆，下一场战斗由服务器重新下发原值即恢复正常（本场已改的不会复原）
- **v3.9.5**：自动开战改为直接点击主页 React 层的"出战"按钮（`button[data-type="game"]`)——与手动点击完全同一链路，主页遮盖会正常切走；修复 v3.9.4 方案（直调 Cocos 层 startHostGame）导致的"主页盖住战斗画面"问题
- **v3.9.4**：修复游戏更新后"自动开战"失效——新版构建强制校验宿主配置，空手调用开局入口会被拒绝（表现为收完结算卡在主页不动）；改为读取游戏内部已注入的宿主配置再开局
- **v3.9.3**：修复"自动下一关"卡在通关结算页——结算弹窗（fightStop）不会自己关，不点"收下"则对局 session 一直活跃、面板一直是战斗模式，两道闸门永远挡着；现在自动代点 fightStop 的"收下"按钮（`collectBtnEvent`，自带防重复锁，复活等付费弹窗结构不同不会误触）
- **v3.9.2**：速度倍率标记为高危功能（面板醒目警告 + 不参与自动重放）：实测加速后捡特殊宝箱/通关结算会因服务器时间校验失败被踢回主页，但碎片金币保留，刷碎片场景可用
- **v3.9.1**：修复状态切换瞬间数据未就绪时面板永久卡在"等待游戏数据加载"（心跳内自动重试渲染）
- **v3.9.0**：自动选卡偏好拆分为两个可组合复选框——"已有优先"与"高级优先"；卡住保护（失败轮换 + 手动让位）转为内置常驻
- **v3.8.3**：自动选卡新增"手动输入保护"（玩家 3 秒内点过屏幕则脚本让位，避免手动/脚本抢点卡死弹窗）；自动选择打 console 日志便于排查
- **v3.8.2**：修复自动开战在结算/上报未完成时打断对局导致"通关失败"（新增 session 活跃与弹窗两道闸门）；自动选卡失败自动轮换下一张，避免卡死
- **v3.8.1**：主页模式直接显示完整自动化开关组（tab 栏主页隐藏时不再够不着配置），进战斗后由 🤖 自动 tab 承接
- **v3.8.0**：新增"窗口失焦不暂停"（默认开）——脚本进驻游戏 iframe 做 Page Visibility 欺骗与失焦事件拦截，解决切 tab/失焦后游戏暂停、挂机中断的问题
- **v3.7.1**：修正"治疗倍率"为"血瓶回血比例"（按最大血量百分比，0.2 = 回 20% 血）；旧倍率语义的存档值自动失效
- **v3.7.0**：新增 🤖 自动 tab，自动化开关集中管理；自动选技能新增"优先升级已有技能"子选项
- **v3.6.0**：新增"升级弹窗自动选第一个技能"开关（默认关闭），与真实点击同一入口，支持折叠面板时生效
- **v3.5.0**：主页模式新增"自动开始下一场战斗"开关（默认关闭），走游戏自身开局入口，带节流与去重
- **v3.4.2**：主页/等待状态隐藏 tab 栏（主页两个 tab 内容相同，避免误导）
- **v3.4.1**：主页模式修复——游客模式警告、"上次战斗"参照、主页数据变化自动刷新（此前主页从不刷新）
- **v3.4.0**：新增"恢复默认属性"按钮、自动重放开关；信息栏移除敌人配置（移至高级页可编辑）
- **v3.3.0**：新增状态持久化（配置自动重放、面板位置/折叠/tab 记忆）；折叠交互修正；技能列表变化检测修正
- **v3.2.1**：配置驱动 + 事件委托重构版
